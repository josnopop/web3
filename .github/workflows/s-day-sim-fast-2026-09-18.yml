name: s-day-sim-fast-2026-09-18
on:
  push:
    branches: [feat/wallet-discovery-v03]
    paths:
      - ".github/workflows/s-day-sim-fast-2026-09-18.yml"
  workflow_dispatch:

concurrency:
  group: s-day-sim-fast-2026-09-18-${{ github.ref }}
  cancel-in-progress: true

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "retired in favor of batch-RPC single-run simulation"
