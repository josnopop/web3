name: sol-alpha-wallet-discovery-ci
on:
  workflow_dispatch:

concurrency:
  group: sol-alpha-wallet-discovery-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  paused:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "auto CI paused while 24h S-wallet simulation is priority"
