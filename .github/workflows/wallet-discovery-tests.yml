name: wallet-discovery-tests
on:
  workflow_dispatch:

concurrency:
  group: wallet-discovery-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  paused:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "auto CI paused while 24h S-wallet simulation is priority"
